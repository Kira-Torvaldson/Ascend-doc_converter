# Roundtrip exclusions

No fixtures are excluded from `verify-roundtrip-adoc-md.js` as of ASC-004.

If a fixture cannot satisfy semantic roundtrip rules (downdoc/Pandoc limitation),
document it here with:

- fixture path
- reason
- tracking issue or release note
