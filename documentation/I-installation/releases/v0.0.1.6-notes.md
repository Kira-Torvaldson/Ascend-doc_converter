## Ascend 0.0.1.6

Patch UX et dev local — polish interface, fond d'écran, CORS Vite.

### Added

- Fond SVG embarqué + support `api/backend/public/rafale.jpg`.
- Composants `ConversionLoadingBanner`, `HeaderStatusPill`.
- Guide assets statiques `api/backend/public/README.md`.

### Changed

- En-tête, panneaux, paramètres (thème clair/sombre).
- CORS : origines `localhost` / `127.0.0.1` en dev.
- Runbook et `container/README.md` (Docker, WSL).

### Fixed

- Fond de page absent sans `rafale.jpg`.
- Erreur CSS PostCSS `.settings-field-error`.

### Validation

```bash
npm run check:version
npm run check:ascend:ci
npm run check:docker:backend
```
